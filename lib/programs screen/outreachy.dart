import 'package:flutter/material.dart';

class outreachy extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('OUTREACHY'),
      ),
      body: Center(
        child: Text('outreachy'),
      ),
    );
  }
}