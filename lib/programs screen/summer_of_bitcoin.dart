import 'package:flutter/material.dart';

class bitcoin extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('Summer of Bitcoin'),
      ),
      body: Center(
        child: Text('Summer of Bitcoin'),
      ),
    );
  }
}
