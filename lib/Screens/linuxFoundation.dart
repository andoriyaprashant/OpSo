import 'package:flutter/material.dart';

class LinuxFoundation extends StatelessWidget {
  const LinuxFoundation({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: const Text('Linux Foundation'),
      ),
      body: const Center(
        child: Text('Linux Foundation Screen'),
      ),
    );
  }
}
